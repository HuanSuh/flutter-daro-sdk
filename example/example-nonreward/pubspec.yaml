name: flutter_daro_sdk_example_nonreward
description: "Demonstrates how to use the flutter_daro_sdk plugin for Non-Reward apps."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  flutter_daro_sdk:
    path: ../../
  
  # daro_core_a:
  #   path: ../../core/daro-core-a

  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
